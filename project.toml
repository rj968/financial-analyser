[project]
name = "stock-analyzer"
version = "0.1.0"
description = "Stock market analysis and backtesting tools"
requires-python = ">=3.14"

dependencies = [
"numpy",
"pandas",
"scipy",
"pyarrow",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
